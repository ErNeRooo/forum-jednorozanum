import styles from "./CreatePostErrorPopup.module.sass";

const CreatePostErrorPopup = () => {
  return (
    <div className={styles.CreatePostErrorPopup}>
      <span>Something went wrong :/</span>
    </div>
  );
};

export default CreatePostErrorPopup;
