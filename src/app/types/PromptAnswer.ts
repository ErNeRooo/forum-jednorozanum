export type PromptAnswer =
  | "program not found"
  | "command not found"
  | "help"
  | "login"
  | "register"
  | "just save command"
  | "wrong login length"
  | "to weak password"
  | "passwords does not match";
